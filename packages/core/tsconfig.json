{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // core 只允许标准 Web API（fetch / crypto），不引入 chrome 或 node 类型
    "lib": ["ES2022", "DOM"],
    "types": []
  },
  "include": ["src"]
}
